export class VecElements extends FunctionNode {
    constructor(description: any, graph: any, valueTypeName: any, elementNames: string[] | undefined, toArray: any);
}
import { FunctionNode } from '@behave-graph/core';
